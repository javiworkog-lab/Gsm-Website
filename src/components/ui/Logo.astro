---
interface Props {
  variant?: 'orange' | 'white' | 'green' | 'mono';
  withTagline?: boolean;
  class?: string;
  size?: number;
}
const { variant = 'white', class: cls = '', size = 56 } = Astro.props;

// Variantes:
// - 'white'  → logo-white-t.png (GSM blanco, fondo transparente). Para fondos oscuros.
// - 'orange' → logo-color-t.png (GSM naranja + tagline verde, fondo transparente). Para fondos claros.
const isOnDark = variant === 'white' || variant === 'mono';
const src = isOnDark ? '/logo-white-t.png' : '/logo-color-t.png';

// Aspect ratios reales de los archivos:
//   logo-color.png: 866 × 608  (≈ 1.42)
//   logo-white.png: 1040 × 494 (≈ 2.10)
const aspect = isOnDark ? 1040 / 494 : 866 / 608;
const width = Math.round(size * aspect);
---

<a href="/" aria-label="GSM — Game Set Match — Inicio" class={`gsm-logo inline-flex items-center ${cls}`}>
  <img
    src={src}
    alt="GSM — Game · Set...Match"
    width={width}
    height={size}
    style={`display:block;height:${size}px;width:auto;`}
    loading="eager"
    decoding="async"
  />
</a>
